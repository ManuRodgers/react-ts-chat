import * as React from 'react';
import Auth from '@/components/Auth';

interface IBossInfoProps {}

const BossInfo: React.FunctionComponent<IBossInfoProps> = props => {
  return (
    <div>
      <Auth /> BossInfo
    </div>
  );
};

export default BossInfo;
